{
  "scripts": {
    "build": "antora antora-playbook.yml && pagefind --site build/site && node scripts/generate-redirects.js",
    "serve": "http-server ./build/site/ -p 4000",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "antora": "3.1.15",
    "http-server": "^14.1.1",
    "pagefind": "^1.5.2"
  }
}
